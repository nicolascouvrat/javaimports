package com.nikodoko.javaimports.testdata.withpackageimports;

public class Test extends Base {
  private List<Structure> function() {
    List<Structure> l = new ArrayList<>();
    for (int i = 0; i < 10; i++) {
      l.add(baseGenerator(i));
    }

    return l;
  }
}
