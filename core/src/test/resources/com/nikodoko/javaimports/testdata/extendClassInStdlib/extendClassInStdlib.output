package extendClassInStdlib;import java.util.AbstractList;

// Assert that we're able to extend a class from the stdlib
class MyList<T> extends AbstractList<T> {
  public void aMethod(T element) {
    add(element);
  }
}
