package com.nikodoko.example;

import static com.google.truth.Truth.assertThat;

public class SomeClass {
  private String param;

  void someFunction(int value) {
    List<String> xs = null;
    int x = value;

    for (int i = 0; i < 10; i++) {
      ImportedClass.staticMethod(i);
    }

    Integer xs = 1;
    assertThat(xs).isNull();
  }
}
